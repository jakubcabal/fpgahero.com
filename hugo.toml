baseURL = 'https://fpgahero.com/'
languageCode = 'en-us'
title = 'The FPGA Hero'
theme = "hugo-bearcub"
copyright = "fpgahero.com (CC BY-SA 4.0)"
# Generate a nice robots.txt for SEO
enableRobotsTXT = true
enableEmoji = true

# Setup syntax highlighting without inline styles. For more information about
# why you'd want to avoid inline styles, see
# https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/style-src#unsafe_inline_styles
[markup]
  [markup.highlight]
    lineNos = true
    lineNumbersInTable = false
    # This allows Bear Cub to use a variation of Dracula that is more accessible
    # to people with poor eyesight. For more information about color contrast
    # and accessibility, see https://web.dev/color-and-contrast-accessibility/
    noClasses = false

[params]
  #favicon = "favicon.png"
  madeWith = "Made with [Hugo](https://gohugo.io/) and [Bear Cub](https://github.com/clente/hugo-bearcub) | Uses [GoatCounter](https://fpgahero.goatcounter.com)"
  title = "The FPGA Hero"
  description = "Blog of experienced FPGA engineers"
  dateFormat = "2006-01-02"

  # (EXPERIMENTAL) This theme has two options for its CSS styles: "original" and
  # "herman". The former is what you see on Bear Cub's demo (an optimized
  # version of Hugo Bear Blog), while the latter has a more modern look based on
  # Herman Martinus's version of the Blogster Minimal theme for Astro.
  themeStyle = "herman"

  # (EXPERIMENTAL) This theme is capable of dynamically generating social cards
  # for posts that don't have `images` defined in their front matter; By setting
  # `generateSocialCard` to false, you can prevent this behavior. For more
  # information see layouts/partials/social_card.html
  generateSocialCard = true

  # Author metadata. This is mostly used for the RSS feed of your site, but the
  # email is also added to the footer of each post. You can hide the "reply to"
  # link by using a `hideReply` param in front matter.
  [params.author]
    name = "The FPGA Hero" # Your name as shown in the RSS feed metadata
    #email = "fpgahero@proton.me" # Added to the footer so readers can reply to posts
